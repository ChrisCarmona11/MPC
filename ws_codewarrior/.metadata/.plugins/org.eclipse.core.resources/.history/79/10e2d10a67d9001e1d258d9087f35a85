/*
 * main implementation: use this 'C' sample to create your own application
 *
 */





#include "derivative.h" /* include peripheral declarations */
#include <stdio.h>

FILE *fprt;

int main(void)
{
	int counter = 0;
	fprt = fopen("constantes_int_1.h","r");
	char content[];
	fseek(fp, 0L, SEEK_END);
	sz = ftell(fp);
	fgets(content,sz , fprt);
	
	for(;;) {	   
	   	counter++;
	}
	
	return 0;
}
